# 📜 CHANGELOG - MemeHunter

> تاریخچه کامل تمام نسخه‌های منتشر شده پروژه **MemeHunter** - ربات پیداکننده میم‌کوین و صدور سیگنال خرید/فروش.

---

## 📋 فهرست مطالب

- [🔧 قوانین پروژه (Project Rules)](#-قوانین-پروژه-project-rules)
- [🏷️ سیستم نسخه‌بندی](#️-سیستم-نسخه‌بندی)
- [📦 تاریخچه نسخه‌ها](#-تاریخچه-نسخه‌ها)
  - [V1.0.0 - اولین نسخه پایدار](#v100---2026-09-14---اولین-نسخه-پایدار-major)
  - [V0.2.x - نسخه‌های توسعه](#v02x---نسخه‌های-توسعه)
  - [V0.1.0 - نسخه اولیه](#v010---2026-09-14---نسخه-اولیه-initial)
- [📊 جدول خلاصه نسخه‌ها](#-جدول-خلاصه-نسخه‌ها)
- [🔄 گردش کار انتشار نسخه جدید](#-گردش-کار-انتشار-نسخه-جدید)

---

## 🔧 قوانین پروژه (Project Rules)

این قوانین **اولویت اول** پروژه هستند و در هر تغییر باید رعایت شوند. تخطی از هر یک از این قوانین غیرقابل‌قبول است.

### قانون 1: نام پروژه

- **نام رسمی**: `MemeHunter`
- **slug (برای نام فایل‌ها و زیپ)**: `memehunter`
- در همه فایل‌ها، README، GitHub Actions و فایل‌های زیپ از این نام استفاده می‌شود.
- تغییر نام پروژه فقط با نسخه Major (مثلاً V2.0.0) مجاز است.

### قانون 2: فولدر `data/` و نگهداری داده

یک فولدر `data/` در ریشه پروژه باید وجود داشته باشد که شامل:

| فایل | توضیح | سیاست نگهداری |
|------|-------|---------------|
| `activity.log` | لاگ فعالیت پروژه (اجراها، خطاها، هشدارها) | **حداقل 90 روز** |
| `activity.log.YYYY-MM-DD` | فایل‌های لاگ چرخشی روزانه | 90 فایل (یک فایل در روز) |
| `coins_database.csv` | دیتابیس ارزهای روز با اندیکاتورها و سیگنال‌ها | **حداقل 90 روز** |

#### ساختار `coins_database.csv`

```
scan_date, scan_timestamp, coin_id, symbol, name, current_price_usd,
market_cap_usd, volume_24h_usd, change_24h_pct, change_7d_pct,
rsi, macd_hist, ma_short, ma_long, ma_bullish, volume_surge_ratio,
price_momentum_pct, score, signal, confidence
```

#### مکانیزم پاکسازی خودکار

- در هر اجرای ربات، رکوردهای قدیمی‌تر از 90 روز از `coins_database.csv` خودکار حذف می‌شوند.
- فایل‌های لاگ قدیمی‌تر از 90 روز خودکار حذف می‌شوند.
- این کار توسط توابع `cleanup_old_db_records()` و `cleanup_old_logs()` در `src/storage.py` و `src/logging_setup.py` انجام می‌شود.

### قانون 3: نسخه‌بندی معنایی (Semantic Versioning)

فرمت: `V{Major}.{Minor}.{Patch}` (حرف **V بزرگ** الزامی است)

| عدد | نام | چه زمانی افزایش یابد | مثال |
|-----|-----|----------------------|------|
| اول | **Major** | بازنویسی کامل، تغییرات شکستن رابط (Breaking Changes)، تغییر نام پروژه | V1.0.0 → V2.0.0 |
| دوم | **Minor** | افزودن ویژگی‌های جدید سازگار با نسخه قبلی | V1.0.0 → V1.1.0 |
| سوم | **Patch** | باگ‌فیکس، بهبودهای کوچک، اصلاح مستندات | V1.0.0 → V1.0.1 |

#### فایل‌های دخیل در نسخه‌بندی

| فایل | توضیح |
|------|-------|
| `VERSION` | فایل متنی شامل نسخه فعلی (مثل `V1.0.0`) |
| `CHANGELOG.md` | این فایل - تاریخچه تغییرات هر نسخه |
| `README.md` | شامل نسخه در هدر و عنوان |
| `.github/workflows/daily-scan.yml` | شامل نسخه در نام job و توضیحات |

### قانون 4: ساخت فایل زیپ با هر انتشار

با هر بار انتشار نسخه (patch/minor/major):

1. فایل `VERSION` به‌روزرسانی شود
2. `README.md` و فایل workflow با نسخه جدید به‌روزرسانی شوند
3. در `CHANGELOG.md` ثبت شود
4. فایل زیپ در مسیر `releases/MemeHunter-V{X.Y.Z}.zip` ساخته شود

#### نام فایل زیپ

```
releases/MemeHunter-V{Major}.{Minor}.{Patch}.zip
```

**مثال**: `MemeHunter-V1.0.0.zip`

#### محتوای زیپ

تمام فایل‌های پروژه به‌جز موارد زیر:
- `__pycache__/`, `*.pyc`, `*.pyo`, `*.log`
- `.git/`
- `releases/` (جلوگیری از نسخه‌بندی تودرتو)
- `venv/`, `env/`, `.venv/`, `.env/`
- `data/` (شامل لاگ و دیتابیس CSV - داده زمان‌مند)
- `reports/` (خروجی موقت گزارش‌ها)

### قانون 5: نگهداری حداکثر 3 نسخه

در پوشه `releases/`:

- **حداکثر 3 نسخه اخیر** نگهداری می‌شود
- نسخه‌های قدیمی‌تر به‌طور خودکار با هر انتشار حذف می‌شوند
- ترتیب بر اساس تاریخ ساخت فایل زیپ (mtime) است
- این کار از شلوغی فولدر جلوگیری می‌کند

#### مثال

```text
releases/
├── MemeHunter-V1.0.2.zip   (جدیدترین)
├── MemeHunter-V1.0.1.zip
└── MemeHunter-V1.0.0.zip   (قدیمی‌ترین قابل نگهداری)
# V0.9.x و قدیمی‌تر خودکار حذف می‌شوند
```

### قانون 6: فقط فایل‌های زیپ نگهداری می‌شوند

- **تمام فایل‌های خارج از پوشه `releases/`** در پروژه اصلی حذف می‌شوند
- کد منبع، مستندات و فایل‌های پیکربندی فقط داخل فایل‌های زیپ نگهداری می‌شوند
- این کار باعث می‌شود پوشه پروژه سبک و تمیز بماند
- برای توسعه یا اجرای ربات، باید آخرین نسخه زیپ را extract کنید

#### استثنا

- فایل `CHANGELOG.md` در ریشه پروژه نگهداری می‌شود (برای دسترسی سریع بدون نیاز به extract)
- فایل `.gitkeep` در پوشه `releases/` برای حفظ ساختار در git

### قانون 7: اولویت رعایت

این قوانین **اولویت اول** هستند:

1. در هر تغییر کد، ابتدا این قوانین رعایت شوند
2. قبل از commit، فایل `VERSION` باید به‌روز باشد
3. لاگ و دیتابیس به‌صورت خودکار در هر اجرا چرخش و پاکسازی می‌شوند
4. هرگز نسخه‌ای بدون ساخت زیپ منتشر نشود
5. هرگز بیش از 3 نسخه در `releases/` نگهداری نشود
6. هرگز فایل کد منبع خارج از زیپ در پروژه اصلی نگهداری نشود (به‌جز `CHANGELOG.md`)

---

## 🏷️ سیستم نسخه‌بندی

پروژه MemeHunter از **Semantic Versioning** با فرمت `V{Major}.{Minor}.{Patch}` استفاده می‌کند.

### جدول کامل انواع تغییرات

| نوع تغییر | مثال | توضیح |
|----------|------|-------|
| Patch | V1.0.0 → V1.0.1 | رفع باگ، بهبود کوچک، اصلاح مستندات |
| Minor | V1.0.0 → V1.1.0 | افزودن ویژگی جدید سازگار با نسخ قبلی |
| Major | V1.0.0 → V2.0.0 | تغییر شکستن رابط، بازنویسی کامل، تغییر نام پروژه |

### دستورات مدیریت نسخه

```bash
# انتشار نسخه جدید
python scripts/release.py patch          # رفع خطا
python scripts/release.py minor          # ویژگی جدید
python scripts/release.py major          # تغییر بزرگ

# مشاهده اطلاعات
python scripts/release.py current        # نسخه فعلی
python scripts/release.py list           # لیست نسخه‌های موجود

# نگهداری
python scripts/release.py cleanup        # حذف نسخه‌های قدیمی
```

---

## 📦 تاریخچه نسخه‌ها



### V1.3.3 - 2026-09-14 - Patch (رفع کرش skipped + Binance 451)

#### 🐛 رفع باگ
- **KeyError: 'skipped'**: در `record_api_call` وقتی Binance در دسترس نبود، وضعیت `skipped` در دیکشنری آمار وجود نداشت و **کل تحلیل هر کوین** با خطا متوقف می‌شد → نتیجه اسکن: ۰ کوین.
- آمار API حالا با `defaultdict(int)` هر وضعیتی را می‌پذیرد (skipped / rate_limited / ...).

#### 🌍 Binance روی GitHub Actions
- HTTP **451** (geo-block) بدون ۵ بار retry طولانی → بلافاصله `None` و ادامه با داده CoinGecko.
- پیش‌فرض endpoint: `https://data-api.binance.vision` (کمتر مسدود می‌شود).
- اسکن حتی بدون Binance هم نتایج واقعی از CoinGecko می‌دهد.

#### ✅ تست
- ۱۰۰ تست پاس، شامل تست جدید `test_logging_stats`.

---

### V1.3.2 - 2026-09-14 - Patch (رفع کرش ATR)

#### 🐛 رفع باگ
- **IndexError در `atr`**: وقتی طول `highs`/`lows` با `closes` فرق داشت (داده market_chart در برابر OHLC)، به خاطر چک اشتباه `len(a) != len(b) != len(c)` در پایتون، حلقه از محدوده خارج می‌شد و اسکن روی GitHub Actions کرش می‌کرد.
- همسان‌سازی طول سری‌ها در `atr` و `compute_indicators`.
- ۳ تست واحد جدید برای mismatch و مسیر سالم.

#### 📝 یادداشت
- این نسخه فقط Patch است (رفع خطا). افزودن KuCoin به‌عنوان منبع مکمل برای نسخه Minor بعدی (V1.4.0) برنامه‌ریزی شده است.

---

### V1.3.1 - 2026-09-14 - Patch (شفافیت کامل و بهبودهای فاخر)

> 🎨 نسخه با یکدست‌سازی کامل تلگرام، شفافیت داده‌های واقعی/پروکسی، و قابلیت‌های جدید برای کاربر حرفه‌ای.

#### ✨ ویژگی‌های جدید

##### فیلتر هوشمند «فقط داده واقعی» (--real-only)
- پرچم جدید `--real-only` برای حالت محافظه‌کارانه
- فقط کوین‌هایی که در Binance لیست شده‌اند نگه داشته می‌شوند
- مناسب کاربرانی که فقط به داده واقعی اعتماد دارند

##### لایه DexScreener برای میم‌های فقط-DEX (--dex-data)
- پرچم جدید `--dex-data`
- دریافت نقدینگی pool، حجم، buys/sells از DexScreener
- برای کوین‌هایی که در Binance لیست نشده‌اند
- استفاده از داده DEX به‌عنوان پروکسی لیکوییدیتی

##### WebSocket برای داده real-time (--use-websocket)
- پرچم جدید `--use-websocket`
- اتصال WebSocket به Binance برای 5 کوین اول
- دریافت real-time: aggTrade (CVD لحظه‌ای) + depth20 (اسپرد لحظه‌ای)
- کتابخانه اختیاری `websocket-client` (اگر نصب نباشد، غیرفعال می‌شود)

##### لاگ ساختاریافته کامل
- اضافه شدن `start_phase` / `end_phase` برای هر مرحله
- `record_api_call` برای آمار جداگانه هر API (CoinGecko/Binance/DexScreener/WebSocket)
- `record_error` برای لیست خطاها در خلاصه نهایی
- `record_data_source` برای شمارش داده واقعی vs پروکسی
- خلاصه JSON اکنون شامل: phases، api_stats، errors، coins_real_data، coins_proxy_only

##### آستانه پویای large trade
- آستانه تشخیص «ترید بزرگ» اکنون بر اساس مارکت‌کپ پویا است:
  - میم‌کوین کوچک (< 100M): آستانه $10K
  - میم‌کوین متوسط (100M-1B): آستانه $25K
  - میم‌کوین بزرگ (> 1B): آستانه $50K
- این یعنی برای میم‌های کوچک‌تر، تریدهای کوچک‌تر هم هوشمند شمرده می‌شوند

##### MTF Confluence با وزن واقعی
- `WEIGHT_MTF_CONFLUENCE` از 0 به 0.05 تغییر کرد
- اکنون وقتی `--enable-advanced-impact` فعال باشد، MTF هم تأثیر دارد
- نرمال‌سازی هوشمند وزن‌ها

##### Paper Trading در تلگرام و HTML
- خلاصه Paper Trading اکنون در پیام تلگرام نمایش داده می‌شود
- بخش مجزا Paper Trading در داشبورد HTML
- نمایش: پوزیشن‌های باز/بسته، نرخ موفقیت، کل PnL، بهترین/بدترین ترید

##### شمارش واقعی vs پروکسی در همه خروجی‌ها
- نمایش «✅ N واقعی | 🔮 M فقط پروکسی» در:
  - خلاصه پیام تلگرام
  - داشبورد HTML
  - خلاصه لاگ JSON
- کاربر همیشه می‌داند چه مقدار داده واقعی دارد

#### 🔧 بهبودها

##### یکدست‌سازی تلگرام
- ایموجی‌های رنگی 🟢/🔴/🟡 در تمام خطوط سیگنال
- ساختار فاخر: هدر + خلاصه + برترین فرصت + جزئیات + Paper Trading + سلب مسئولیت
- سلب مسئولیت ثابت و کوتاه در پایان هر پیام
- قالب‌بندی فشرده‌تر با ایموجی رنگی برای هر اندیکاتور

##### برچسب همیشه‌واضح Sweep
- Sweep همیشه به‌عنوان «🔮 پروکسی (OHLC-based)» برچسب می‌خورد
- در تمام خروجی‌ها: تلگرام، HTML، CSV، JSON

##### نرمال‌سازی واضح‌تر وزن‌ها
- وقتی `--enable-advanced-impact` روشن است، وزن‌ها به‌صورت هوشمند نرمال می‌شوند
- مجموع وزن‌ها می‌تواند > 1.0 باشد چون وزن‌های پیشرفته فقط در این حالت اعمال می‌شوند
- نرمال‌سازی خودکار بر اساس مجموع وزن‌های فعال

##### همگام‌سازی RELEASE_RULES.md
- به‌روزرسانی به 39 ستون CSV (V1.3.0)
- جدول کامل منابع داده پشتیبانی‌شده
- توضیح برچسب‌های `is_real`

#### 📊 آمار

- **ماژول‌های جدید**: 2 (websocket_stream، dex_provider)
- **تعداد فایل‌ها**: 50 فایل (افزایش از 48)
- **تست‌های واحد**: 96 تست (همه موفق) + 8 تست انتگراسیون
- **پرچم‌های جدید CLI**: 4 (--real-only، --dex-data، --use-websocket، --enable-advanced-impact بهبود یافته)

#### 📌 نکات مهم ارتقا

- این نسخه با V1.3.0 کاملاً سازگار است
- برای حالت محافظه‌کارانه: `python main.py --real-data --real-only --advanced`
- برای پوشش کامل (CEX + DEX): `python main.py --real-data --dex-data --advanced`
- برای داده real-time: `pip install websocket-client` سپس `python main.py --use-websocket`

---

### V1.3.0 - 2026-09-14 - Minor (داده واقعی Binance + شفافیت کامل)

> 🎯 بزرگترین تغییر معماری - اضافه شدن لایه داده واقعی Binance برای OrderFlow/Liquidity واقعی، Paper Trading، برچسب پروکسی/واقعی، و شفافیت کامل محدودیت‌ها.

#### ✨ ویژگی‌های جدید

##### لایه داده واقعی Binance Public API
- **ماژول جدید `src/binance_provider.py`**
- کاملاً رایگان، **بدون نیاز به کلید API**
- `aggTrades` → محاسبه واقعی CVD (Cumulative Volume Delta)
- `depth` → اسپرد و عمق واقعی order book
- `klines` → OHLCV با بازه‌های مختلف (1m تا 1w)
- تشخیص خودکار کوین‌های لیست‌شده در Binance
- فقط کوین‌های CEX (مثل PEPE، WIF، DOGE) پشتیبانی می‌شوند

##### برچسب «پروکسی/واقعی» در تمام خروجی‌ها
- اضافه شدن فیلد `is_real` و `source` در تمام نتایج تحلیل پیشرفته
- نمایش برچسب در خروجی HTML، تلگرام، لاگ و CSV
- کاربر همیشه می‌داند داده واقعی است یا تقریبی
- جدول شفافیت در README

##### Paper Trading ساده
- **ماژول جدید `src/paper_trading.py`**
- باز کردن خودکار پوزیشن برای سیگنال‌های خرید قوی
- بررسی خودکار SL/TP در هر اسکن
- ذخیره در `data/paper_trades.csv`
- آمار کامل: win rate، کل PnL، بهترین/بدترین ترید
- دستور `python main.py --paper-trading` برای گزارش

##### تأثیر اختیاری تحلیل پیشرفته روی امتیاز
- پرچم جدید `--enable-advanced-impact`
- تأثیر کم (وزن 0.05 برای Liquidity و OrderFlow)
- قابل خاموش کردن (پیش‌فرض خاموش - محافظه‌کارانه)
- نرمال‌سازی هوشمند وزن‌ها

##### Multi-Timeframe Confluence واقعی
- اتصال داده چند بازه از Binance به امتیاز
- آستانه confluence قابل تنظیم (حداکثر 2 بازه هم‌جهت)
- محاسبه امتیاز بر اساس تعداد بازه‌های هم‌جهت

##### بک‌تست با فاصله اطمینان و تعداد نمونه
- اضافه شدن `buy_sample_count_7d` و `buy_sample_count_14d`
- محاسبه فاصله اطمینان 95% (z=1.96)
- نمایش در گزارش `--backtest`
- اعتبار آماری نتایج

##### غنی‌سازی لاگ اجرا
- **خلاصه JSON در انتهای هر اجرا** (`RUN_SUMMARY_JSON:`)
- لاگ تحلیل هر کوین به‌صورت JSON (`COIN_ANALYSIS_JSON:`)
- لاگ وضعیت منابع داده (`DATA_SOURCE_JSON:`)
- قابل پردازش توسط ابزارهای بعدی (grep، jq، داشبورد)

##### ساختار فاخر پیام تلگرام
- ایموجی‌های یکدست و حرفه‌ای
- نمایش برچسب پروکسی/واقعی برای هر تحلیل
- بخش "برترین فرصت" با منابع داده
- خلاصه فشرده اندیکاتورها با ایموجی
- قالب‌بندی مبالغ بزرگ به K/M/B

##### ذخیره فیلدهای کلیدی پیشرفته در CSV
- دیتابیس اکنون 38 ستون دارد (افزایش از 24)
- ستون‌های جدید: `liquidity_score`, `liquidity_is_real`, `liquidity_spread`, `liquidity_imbalance`, `sweep_detected`, `sweep_type`, `orderflow_buy_pressure`, `orderflow_sell_pressure`, `orderflow_is_real`, `large_swap_count`, `large_buys`, `large_sells`, `mtf_confluence_score`, `mtf_aligned_count`, `data_sources`

##### CI کامل در GitHub Actions
- **Job 1: Lint** (pycodestyle برای خطاهای بحرانی)
- **Job 2: Tests** (unittest + pytest اختیاری)
- **Job 3: Version Check** (بررسی فرمت VERSION و CHANGELOG)
- **Job 4: Scheduled Scan** (فقط در schedule)

##### پشتیبانی از API تخصصی OrderFlow
- متغیرهای `ORDERFLOW_API_KEY` و `ORDERFLOW_API_URL` در config
- برای کاربرانی که سرویس تخصصی دارند (Kaiko، Amberdata، ...)

##### تست‌های واحد بیشتر
- اضافه شدن 26 تست جدید (از 70 به 96 تست)
- تست‌های کامل برای: Binance provider، Paper Trading، Backtest V1.3.0

#### 🔧 بهبودها

##### کاهش `except Exception` های گسترده
- تمام except ها به استثناهای مشخص محدود شدند
- `requests.exceptions.RequestException`, `ValueError`, `KeyError`, `OSError`, `RuntimeError`
- بهبود قابل توجه در debug و رفع خطا

##### مستندسازی وزن‌ها
- کامنت‌های کامل برای هر وزن در config.py
- جدول واضح برای 7 فاکتور اصلی + 3 فاکتور پیشرفته
- توضیح نرمال‌سازی

##### به‌روزرسانی RELEASE_RULES.md
- جدول کامل ستون‌های CSV (38 ستون)
- جدول منابع داده پشتیبانی‌شده
- توضیح برچسب‌های `is_real`

#### 🐛 رفع باگ‌ها

- رفع مشکل عدم تأثیر تحلیل پیشرفته روی امتیاز نهایی
- رفع مشکل محاسبه ATR وقتی highs/lows خالی هستند
- بهبود مدیریت خطا در Binance API (جلوگیری از crash در صورت 400)

#### 📊 آمار

- **تعداد فایل‌ها**: 50 فایل (افزایش از 43 فایل)
- **خطوط کد**: ~3500 خط پایتون (افزایش از 2800 خط)
- **تست‌های واحد**: 96 تست (افزایش از 70 تست)
- **ماژول‌های جدید**: 2 ماژول (binance_provider، paper_trading)
- **ستون‌های CSV**: 38 ستون (افزایش از 24)

#### 📌 نکات مهم ارتقا

- این نسخه با V1.2.0 کاملاً سازگار است
- برای فعال‌سازی داده واقعی: `python main.py --real-data --advanced`
- برای فعال‌سازی Paper Trading: به‌صورت پیش‌فرض در هر اسکن فعال است
- برای فعال‌سازی تأثیر امتیاز: `python main.py --enable-advanced-impact`
- دیتابیس قبلی CSV با ستون‌های جدید هماهنگ نیست - هدر به‌روز می‌شود

---

### V1.2.0 - 2026-09-14 - Minor (تحلیل‌های پیشرفته و بک‌تست)

> 🚀 بزرگترین بهبود پروژه - اضافه شدن 4 تحلیل پیشرفته، بک‌تست، هشدار حجم، داشبورد HTML و Token Bucket Rate Limiter.

#### ✨ ویژگی‌های جدید

##### 4 تحلیل پیشرفته (Advanced Analysis)
- **لیکوییدیتی (Liquidity)**: تحلیل نقدشوندگی بر اساس حجم 30 روزه، تخمین اسپرد
- **سوییپ (Sweep)**: تشخیص سوییپ لیکوییدیتی (شکست سقف/کف و بازگشت) - نشانه حرکت پول هوشمند
- **اردرفلو (Order Flow)**: تحلیل فشار خرید/فروش، دلتای تجمعی، تشخیص جذب سفارش (Absorption)
- **والیوم پروفایل (Volume Profile)**: محاسبه POC، Value Area (VAH/VAL)، تشخیص شکل پروفایل (P/B/D/Normal)

##### بک‌تست سیگنال‌ها (Backtest)
- ماژول جدید `src/backtest.py`
- بررسی عملکرد سیگنال‌های خرید بعد از 7 و 14 روز
- محاسبه نرخ موفقیت (win rate) و میانگین بازده
- شناسایی بهترین و بدترین سیگنال‌ها
- دستور `python main.py --backtest` برای اجرای مستقل

##### هشدار حجم غیرعادی (Volume Anomaly Alert)
- ماژول جدید `src/volume_alert.py`
- تشخیص حجم غیرعادی با دو روش آماری:
  - نسبت ساده: حجم فعلی / میانگین 30 روز
  - z-score: انحراف معیار آماری
- 4 سطح شدت: low، medium، high، extreme
- دستور `python main.py --volume-alert` برای اجرای مستقل

##### داشبورد HTML
- ماژول جدید `src/html_reporter.py`
- خروجی HTML با طراحی مدرن (gradient، cards، responsive)
- نمایش امتیاز با نوار رنگی
- شامل خلاصه آماری و بک‌تست
- دستور `python main.py --format html --save`

##### محدودیت نرخ پیشرفته (Token Bucket)
- ماژول جدید `src/rate_limiter.py`
- الگوریتم Token Bucket با capacity=10 و refill_rate=0.5/s
- کنترل دقیق نرخ درخواست‌های API
- قابل تنظیم و غیرفعال‌سازی

##### نرمال‌سازی MACD با ATR
- MACD اکنون بر اساس ATR نرمال‌سازی می‌شود (نه فقط tanh)
- این یعنی امتیاز MACD اکنون نسبت به نوسان واقعی بازار سنجیده می‌شود
- رفع مشکل قبلی که در کوین‌های پرنوسان، MACD امتیاز غلط می‌داد

##### تأثیر صریح ATR و Bollinger روی امتیاز
- ATR اکنون وزن مستقل 0.10 در امتیاز دارد (نوسان متوسط = خوب)
- Bollinger Bands اکنون وزن مستقل 0.15 در امتیاز دارد (position-based)
- اکنون 7 فاکتور با وزن برابر تقریباً در امتیاز نهایی تاثیر دارند

##### بهبود قالب پیام تلگرام
- ایموجی‌های واضح برای هر سیگنال
- خلاصه سریع در ابتدای پیام
- بخش "برترین فرصت" (اگر BUY داریم)
- نمایش فشرده اندیکاتورها با ایموجی
- قالب‌بندی مبالغ بزرگ به K/M/B

##### پشتیبانی از چند بازه زمانی
- متد جدید `get_coin_market_chart_by_timeframe` در CoinGeckoClient
- پشتیبانی از بازه‌های روزانه، 4 ساعته و ساعتی
- قابل تنظیم در config.py با `TIMEFRAMES`

##### گزارش عملکرد در --format detailed
- اکنون در خروجی detailed، خلاصه بک‌تست سیگنال‌های گذشته نمایش داده می‌شود
- شامل نرخ موفقیت، میانگین بازده، بهترین و بدترین سیگنال

##### تست‌های واحد بیشتر
- اضافه شدن 26 تست جدید (از 44 به 70 تست)
- تست‌های کامل برای: liquidity، sweep، orderflow، volume profile، rate limiter، backtest، volume alert
- همه 70 تست موفق هستند

#### 🔧 بهبودها

##### وابستگی‌ها
- اضافه شدن `pytest>=8.0.0` به requirements.txt (اختیاری، چون با unittest هم کار می‌کند)

##### معماری
- اضافه شدن ماژول `advanced_analysis.py` برای تجمیع تحلیل‌های پیشرفته
- اضافه شدن ماژول `rate_limiter.py` به‌عنوان لایه جداگانه
- اضافه شدن ماژول `backtest.py` برای ارزیابی عملکرد
- اضافه شدن ماژول `volume_alert.py` برای هشدار حجم
- اضافه شدن ماژول `html_reporter.py` برای خروجی HTML

#### 📊 آمار

- **تعداد فایل‌ها**: 42 فایل (افزایش از 34 فایل)
- **خطوط کد**: ~2800 خط پایتون (افزایش از 1800 خط)
- **تست‌های واحد**: 70 تست (افزایش از 44 تست)
- **ماژول‌های جدید**: 5 ماژول (advanced_analysis، rate_limiter، backtest، volume_alert، html_reporter)
- **وابستگی‌ها**: `requests`, `python-dotenv`, `pytest`

#### 📌 نکات مهم ارتقا

- این نسخه با V1.1.0 کاملاً سازگار است
- دیتابیس قبلی `coins_database.csv` نیازی به تغییر ندارد
- برای استفاده از تحلیل‌های پیشرفته: `python main.py --advanced`
- برای مشاهده داشبورد HTML: `python main.py --format html --save`
- برای بک‌تست: `python main.py --backtest`
- برای هشدار حجم: `python main.py --volume-alert`

---

### V1.1.0 - 2026-09-14 - Minor (بهبودهای فنی)

> 🔧 نسخه با بهبودهای فنی قابل توجه و رفع 8 نقطه ضعف گزارش‌شده.

#### ✨ ویژگی‌های جدید

##### بارگذاری پویای نسخه
- نسخه اکنون از فایل `VERSION` در زمان اجرا خوانده می‌شود (در `config.py`)
- User-Agent هدر HTTP اکنون شامل نسخه واقعی است: `memehunter/V1.1.0`
- رفع ناسازگاری قبلی که `PROJECT_VERSION` به‌صورت ثابت `V0.1.0` بود

##### اندیکاتورهای جدید پیشرفته
- **ATR (Average True Range)** با روش ویلدر - نوسان بازار
- **Bollinger Bands** با period=20 و std_dev=2 - تشخیص اشباع خرید/فروش و squeeze
- هر دو در دیتابیس CSV ذخیره می‌شوند (ستون‌های `atr`, `atr_percent`, `bollinger_percent_b`, `bollinger_squeeze`)

##### بهبود RSI
- اکنون از **روش هموارسازی ویلدر (Wilder's smoothing)** استفاده می‌کند (استاندارد صنعتی)
- در نسخه قبلی از روش ساده استفاده می‌شد که با پلتفرم‌های معاملاتی استاندارد سازگار نبود

##### کش (cache) برای کاهش درخواست‌های API
- اضافه شدن کلاس `FileCache` در `src/coin_service.py`
- ذخیره پاسخ‌های CoinGecko در `data/cache/` با TTL 1 ساعت
- کاهش چشمگیر درخواست‌های تکراری به API
- قابل غیرفعال‌سازی با `ENABLE_CACHE=False`

##### فیلتر پیشرفته میم‌کوین
- استفاده از **دسته‌بندی‌های CoinGecko**: `meme-token`, `dog-themed-coins`, `cat-themed-coins`, `pump-fun`
- حفظ فیلتر کلمات کلیدی به‌عنوان پشتیبان
- افزایش دقت تشخیص میم‌کوین‌ها (از 100 کوین برتر به 180+ کوین از دسته‌ها)

##### پشتیبانی از کلید API CoinGecko
- متغیر محیطی `COINGECKO_API_KEY` برای رفع محدودیت نرخ رایگان
- پشتیبانی از کلیدهای Demo و Pro

##### پشتیبانی از python-dotenv
- بارگذاری خودکار متغیرهای محیطی از فایل `.env`
- اضافه شدن فایل `.env.example` به‌عنوان الگو
- اگر نصب نباشد، ربات بدون مشکل کار می‌کند

##### وزن‌دهی کامل امتیاز
- اضافه شدن وزن **MA Trend** به امتیاز نهایی (0.20)
- اکنون 5 فاکتور با وزن برابر: RSI + MACD + Volume + Momentum + MA
- رفع مشکل قبلی که MA فقط در دلایل می‌آمد ولی در امتیاز تاثیر نداشت

##### تست‌های واحد
- اضافه شدن پوشه `tests/` با 44 تست واحد
- تست‌های کامل برای: RSI, MACD, MA, Volume Surge, ATR, Bollinger Bands, امتیازدهی، فیلتر میم‌کوین و کش
- اجرا با `python -m unittest discover tests -v`

#### 🐛 رفع باگ‌ها

- **رفع باگ جدی در MACD**: در نسخه قبلی، EMA سریع و کند با فازهای متفاوتی zip می‌شدند که باعث می‌شد macd_line همیشه منفی باشد حتی در روند صعودی قوی. با این باگ، MACD عملاً هیچ وقت سیگنال خرید نمی‌داد!
- **رفع باگ در `parse_kind`** (از V1.0.0): حرف `M` بزرگ به‌جای اینکه برای major باشد، با `m` کوچک برای minor اشتباه گرفته می‌شد.
- **رفع باگ در `EXCLUDE_DIRS`** (از V1.0.0): پوشه `reports/` در فایل زیپ قرار می‌گرفت.

#### 🔧 بهبودها

##### ساختار پروژه کامل در زیپ
- اضافه شدن `data/.gitkeep` و `reports/.gitkeep` به فایل زیپ
- اکنون ساختار پروژه در ریپو کامل دیده می‌شود

##### بهبود .gitignore
- اضافه شدن `.env` و `data/cache/` به فایل‌های نادیده گرفته شده

#### 📊 آمار

- **تعداد فایل‌ها**: 28 فایل (افزایش از 20 فایل)
- **خطوط کد**: حدود 1800 خط پایتون (افزایش از 1239 خط)
- **تست‌های واحد**: 44 تست (همه موفق)
- **وابستگی‌ها**: `requests`, `python-dotenv`

#### 📌 نکات مهم ارتقا

- اگر از V1.0.0 ارتقا می‌دهید، فقط کافیست فایل‌های جدید را جایگزین کنید
- برای فعال‌سازی تلگرام: `cp .env.example .env` و سپس مقادیر را پر کنید
- برای کاهش rate-limit: کلید رایگان از [coingecko.com/api](https://www.coingecko.com/api) بگیرید و در `.env` قرار دهید
- دیتابیس قبلی `coins_database.csv` با ستون‌های جدید هماهنگ نیست - باید حذف یا backup شود

---

### V1.0.0 - 2026-09-14 - اولین نسخه پایدار (Major)

> 🎉 اولین انتشار رسمی و پایدار پروژه MemeHunter.

#### ✨ ویژگی‌های جدید

##### کشف میم‌کوین
- دریافت خودکار 100 کوین برتر بازار از CoinGecko API
- فیلتر هوشمند بر اساس کلمات کلیدی (doge, shib, pepe, floki, elon, moon, inu, bonk, wif, ...)
- فیلتر استیبل‌کوین‌ها (USDT, USDC, DAI, BUSD, ...)
- فیلتر مالی: مارکت‌کپ < 5 میلیارد دلار، حجم 24h > 1 میلیون دلار

##### اندیکاتورهای تکنیکال
- **RSI** (Relative Strength Index) با دوره 14 روز - تشخیص اشباع خرید/فروش
- **MACD** (12/26/9) - تشخیص روند و فشار خریداران/فروشندگان
- **Moving Average** (7/21 روز) - روند کوتاه و بلندمدت
- **Volume Surge** (14 روز) - نسبت حجم امروز به میانگین حجم اخیر
- **Price Momentum** (7 روز) - درصد تغییر قیمت اخیر

##### امتیازدهی و سیگنال
- امتیازدهی چندفاکتوری با وزن برابر (0.25 برای هر فاکتور)
- آستانه‌های قابل تنظیم:
  - امتیاز ≥ 0.65 → سیگنال **خرید**
  - امتیاز 0.35-0.65 → سیگنال **نگه‌داری**
  - امتیاز ≤ 0.35 → سیگنال **فروش**
- نمایش دلایل شفاف به زبان فارسی برای هر سیگنال

##### خروجی و گزارش‌گیری
- 4 قالب خروجی: جدول ترمینال، گزارش مفصل، JSON، CSV
- ذخیره گزارش در فایل با پرچم `--save`

##### دیتابیس و لاگ
- دیتابیس CSV در `data/coins_database.csv` با 20 ستون
- لاگ فعالیت چرخشی روزانه در `data/activity.log`
- نگهداری خودکار 90 روز برای هر دو
- پاکسازی خودکار رکوردهای قدیمی در هر اجرا

##### نوتیفیکیشن تلگرام
- ارسال خودکار سیگنال‌ها به کانال تلگرام
- پیش‌نمایش پیام با `--telegram-preview` بدون ارسال واقعی
- پشتیبانی از پیام‌های طولانی (تقسیم به چند بخش)

##### خودکارسازی
- GitHub Actions هر 6 ساعت یک‌بار اجرا می‌شود
- ذخیره گزارش به‌عنوان artifact با نگهداری 90 روز
- پشتیبانی از Secrets برای تلگرام

##### مدیریت نسخه
- سیستم نسخه‌بندی معنایی `V{Major}.{Minor}.{Patch}`
- اسکریپت `scripts/release.py` برای انتشار خودکار
- ساخت خودکار فایل زیپ با هر انتشار
- نگهداری حداکثر 3 نسخه اخیر

#### 🐛 رفع باگ‌ها

- **باگ در `parse_kind`**: حرف `M` بزرگ به‌جای اینکه برای major باشد، با `m` کوچک برای minor اشتباه گرفته می‌شد. رفع شد.
- **باگ در `EXCLUDE_DIRS`**: پوشه `reports/` در فایل زیپ قرار می‌گرفت. به لیست مستثنی اضافه شد.

#### 📊 آمار

- **تعداد فایل‌ها**: 20 فایل
- **حجم زیپ**: 33.3 KB
- **خطوط کد**: حدود 1239 خط پایتون
- **وابستگی‌ها**: فقط `requests`

#### 📌 یادداشت‌ها

- این نسخه اولین نسخه پایدار رسمی است
- قبل از این نسخه، نسخه‌های V0.1.0 تا V0.2.3 در فاز توسعه بودند
- API رایگان CoinGecko محدودیت نرخ درخواست دارد (10-30 درخواست در دقیقه)

---

### V0.2.x - نسخه‌های توسعه

#### V0.2.3 - 2026-09-14 - Patch

##### تغییرات
- به‌روزرسانی README.md و GitHub Actions workflow با نسخه جدید
- ساخت فایل زیپ `MemeHunter-V0.2.3.zip`

##### توضیح
این نسخه سومین patch در سری V0.2.x بود و برای تست سیستم نگهداری نسخه‌ها (حداکثر 3 نسخه) استفاده شد. با انتشار این نسخه، نسخه V0.2.0 خودکار حذف شد.

---

#### V0.2.2 - 2026-09-14 - Patch

##### تغییرات
- به‌روزرسانی README.md و GitHub Actions workflow
- ساخت فایل زیپ `MemeHunter-V0.2.2.zip`

##### توضیح
این نسخه دومین patch در سری V0.2.x بود.

---

#### V0.2.1 - 2026-09-14 - Patch

##### تغییرات
- به‌روزرسانی README.md و GitHub Actions workflow
- ساخت فایل زیپ `MemeHunter-V0.2.1.zip`

##### توضیح
این نسخه اولین patch در سری V0.2.x بود.

---

#### V0.2.0 - 2026-09-14 - Minor

##### ✨ ویژگی‌های جدید
- اضافه شدن ماژول `version.py` برای مدیریت نسخه‌بندی معنایی
- اضافه شدن ماژول `logging_setup.py` برای لاگ چرخشی 90 روزه
- اضافه شدن ماژول `storage.py` برای دیتابیس CSV
- اضافه شدن اسکریپت `scripts/release.py` برای انتشار خودکار نسخه
- ساخت خودکار فایل زیپ با هر انتشار
- نگهداری حداکثر 3 نسخه اخیر
- فایل `CHANGELOG.md` برای ثبت تاریخچه
- فایل `RELEASE_RULES.md` برای مستندسازی قوانین
- به‌روزرسانی `main.py` برای استفاده از لاگ و دیتابیس

##### 🔧 بهبودها
- نمایش نسخه در هدر ربات هنگام اجرا
- نمایش مسیر فایل‌های لاگ و دیتابیس در پایان اجرا
- نمایش آمار دیتابیس (تعداد رکوردها، کوین‌های منحصر، آخرین اسکن)
- پاکسازی خودکار رکوردهای قدیمی در هر اجرا

---

### V0.1.0 - 2026-09-14 - نسخه اولیه (Initial)

> 🚀 اولین نسخه پروژه MemeHunter.

#### ✨ ویژگی‌های اولیه

##### ماژول‌های اصلی
- `src/config.py` - تنظیمات پروژه
- `src/coin_service.py` - اتصال به CoinGecko API
- `src/indicators.py` - اندیکاتورهای تکنیکال (RSI, MACD, MA, Volume, Momentum)
- `src/analyzer.py` - ترکیب اندیکاتورها و صدور سیگنال
- `src/reporter.py` - قالب‌بندی خروجی (table, detailed, json, csv)

##### قابلیت‌ها
- دریافت 100 کوین برتر از CoinGecko
- فیلتر میم‌کوین‌ها با کلمات کلیدی
- محاسبه 5 اندیکاتور تکنیکال
- امتیازدهی چندفاکتوری
- صدور سیگنال خرید/فروش/نگه‌داری
- 4 قالب خروجی

##### رابط خط فرمان
- `--format` برای انتخاب قالب خروجی
- `--limit` برای محدود کردن تعداد کوین‌ها
- `--save` برای ذخیره گزارش در فایل
- `--verbose` برای لاگ‌های جزئی‌تر

##### مستندات
- `README.md` کامل به زبان فارسی با راهنمای نصب و استفاده
- `LICENSE` به‌صورت MIT
- `.gitignore` استاندارد پایتون

##### GitHub Actions
- workflow `daily-scan.yml` برای اجرای هر 6 ساعت
- آپلود گزارش به‌عنوان artifact

#### 📊 آمار

- **تعداد فایل‌ها**: 13 فایل
- **خطوط کد**: حدود 800 خط پایتون

---

## 📊 جدول خلاصه نسخه‌ها

| نسخه | تاریخ | نوع | تغییرات کلیدی | حجم زیپ |
|------|-------|-----|---------------|---------|
| V0.1.0 | 2026-09-14 | Initial | ساختار اولیه پروژه | ~30 KB |
| V0.2.0 | 2026-09-14 | Minor | افزودن نسخه‌بندی، لاگ، دیتابیس، اسکریپت release | ~33 KB |
| V0.2.1 | 2026-09-14 | Patch | به‌روزرسانی نسخه‌ها | ~33 KB |
| V0.2.2 | 2026-09-14 | Patch | به‌روزرسانی نسخه‌ها | ~33 KB |
| V0.2.3 | 2026-09-14 | Patch | به‌روزرسانی نسخه‌ها | 32.7 KB |
| V1.0.0 | 2026-09-14 | Major | اولین نسخه پایدار | 33.3 KB |
| V1.1.0 | 2026-09-14 | Minor | بهبودهای فنی + ATR + Bollinger + Cache + تست‌ها | ~40 KB |
| V1.2.0 | 2026-09-14 | Minor | تحلیل‌های پیشرفته + بک‌تست + داشبورد HTML | ~60 KB |
| V1.3.0 | 2026-09-14 | Minor | داده واقعی Binance + Paper Trading + شفافیت | ~85 KB |
| **V1.3.1** | **2026-09-14** | **Patch** | **DexScreener + WebSocket + فیلتر real-only + شفافیت کامل** | **~110 KB** |

> **نکته**: نسخه‌های V0.2.0، V0.2.1 و V0.2.2 به دلیل قانون نگهداری حداکثر 3 نسخه حذف شده‌اند. در حال حاضر فقط V1.0.0 و V0.2.3 موجود هستند.

---

## 🔄 گردش کار انتشار نسخه جدید

### مراحل کامل

```bash
# 1. استخراج آخرین نسخه از پوشه releases
cd /home/z/my-project/download/meme-coin-bot
unzip releases/MemeHunter-V1.0.0.zip -d /tmp/work
cd /tmp/work/memehunter

# 2. نصب وابستگی‌ها
pip install -r requirements.txt

# 3. اجرای ربات برای تست
python main.py --limit 5 --telegram-preview

# 4. ویرایش کد (در صورت نیاز)
# ... ویرایش فایل‌ها ...

# 5. انتشار نسخه جدید
python scripts/release.py patch   # یا minor / major

# 6. کپی زیپ جدید به پوشه اصلی پروژه
cp /tmp/work/memehunter/releases/MemeHunter-V{new_version}.zip \
   /home/z/my-project/download/meme-coin-bot/releases/

# 7. کپی CHANGELOG.md به‌روزرسانی شده به ریشه پروژه
cp /tmp/work/memehunter/CHANGELOG.md \
   /home/z/my-project/download/meme-coin-bot/CHANGELOG.md

# 8. پاکسازی فایل‌های موقت
rm -rf /tmp/work
```

### قالب ورودی CHANGELOG برای نسخه جدید

هنگام افزودن ورودی جدید به این فایل، از این قالب استفاده کنید:

```markdown
## V{X.Y.Z} - {YYYY-MM-DD} - {Major/Minor/Patch} ({عنوان کوتاه})

{یک پاراگراف توضیح کلی}

### ✨ ویژگی‌های جدید
- ویژگی 1
- ویژگی 2

### 🐛 رفع باگ‌ها
- باگ 1
- باگ 2

### 🔧 بهبودها
- بهبود 1
- بهبود 2

### 💥 تغییرات شکستن رابط (Breaking Changes)
*(فقط برای نسخه‌های Major)*
- تغییر 1
- تغییر 2

### 📊 آمار
- تعداد فایل‌ها: N فایل
- حجم زیپ: N KB
- خطوط کد: N خط
```

---

## 📞 پشتیبانی

برای سوالات، گزارش باگ یا پیشنهادات:

1. یک issue در گیت‌هاب باز کنید
2. یا مستقیماً با توسعه‌دهنده تماس بگیرید

---

## ⚠️ سلب مسئولیت

این پروژه صرفاً جنبه آموزشی دارد و هیچ‌گونه توصیه مالی نیست. نویسنده هیچ مسئولیتی در قبال زیان‌های احتمالی ناشی از استفاده از این ابزار ندارد. همیشه قبل از هر سرمایه‌گذاری، تحقیق شخصی (DYOR) انجام دهید.

---

> **آخرین به‌روزرسانی**: 2026-09-14
> **نسخه فعلی**: V1.0.0
> **تعداد کل نسخه‌های منتشر شده**: 6 (V0.1.0 تا V1.0.0)
